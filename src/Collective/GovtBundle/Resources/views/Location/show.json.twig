{
    "type"  : "Location",
    "title" : "{{ location }}",
    "links": [
    {% for l in location.links %} 
    { 
        "type": "link",
        "title": "{{ l.title }}",
        "link": "{{ l.link }}"
    }{% if not loop.last %},{% endif %}{% endfor %}]
}