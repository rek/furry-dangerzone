{
    "type": "FeatureCollection",
    "features": [
{% for d in data %}    
    { 
        "type": "Feature",
        "geometry": { 
            "type": "Point", 
            "coordinates": [{{ d.lon }}, {{ d.lat }}]
        }, 
        "properties": {
            "id"   : "{{ d.id }}",
            "name" : "{{ d.name }}",
            "icon" : "{{ d.icon }}"
        }
    }{% if not loop.last %},{% endif %}{% endfor %}]
}